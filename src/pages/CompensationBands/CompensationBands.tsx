// modules
import React from 'react';
// components
import { Page } from '../../components/Page';

const CompensationBands: React.FC = () => {
	return (
		<Page withTitle>
			<div>Compensation Bands body</div>
		</Page>
	);
};

export default CompensationBands;
