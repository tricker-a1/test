// modules
import React from 'react';
// components
import { Page } from '../../components/Page';

const CompensationCycles: React.FC = () => {
	return (
		<Page withTitle>
			<div>Compensation Cycles body</div>
		</Page>
	);
};

export default CompensationCycles;
